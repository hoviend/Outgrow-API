// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"outgrow/ent/mastereventtype"
	"outgrow/ent/schema"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// MasterEventType is the model entity for the MasterEventType schema.
type MasterEventType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Rules holds the value of the "rules" field.
	Rules []schema.EventRules `json:"rules,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MasterEventType) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case mastereventtype.FieldRules:
			values[i] = new([]byte)
		case mastereventtype.FieldID:
			values[i] = new(sql.NullInt64)
		case mastereventtype.FieldName, mastereventtype.FieldDescription:
			values[i] = new(sql.NullString)
		case mastereventtype.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MasterEventType fields.
func (met *MasterEventType) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case mastereventtype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			met.ID = int(value.Int64)
		case mastereventtype.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				met.Name = value.String
			}
		case mastereventtype.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				met.Description = value.String
			}
		case mastereventtype.FieldRules:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field rules", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &met.Rules); err != nil {
					return fmt.Errorf("unmarshal field rules: %w", err)
				}
			}
		case mastereventtype.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				met.CreatedAt = value.Time
			}
		default:
			met.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MasterEventType.
// This includes values selected through modifiers, order, etc.
func (met *MasterEventType) Value(name string) (ent.Value, error) {
	return met.selectValues.Get(name)
}

// Update returns a builder for updating this MasterEventType.
// Note that you need to call MasterEventType.Unwrap() before calling this method if this MasterEventType
// was returned from a transaction, and the transaction was committed or rolled back.
func (met *MasterEventType) Update() *MasterEventTypeUpdateOne {
	return NewMasterEventTypeClient(met.config).UpdateOne(met)
}

// Unwrap unwraps the MasterEventType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (met *MasterEventType) Unwrap() *MasterEventType {
	_tx, ok := met.config.driver.(*txDriver)
	if !ok {
		panic("ent: MasterEventType is not a transactional entity")
	}
	met.config.driver = _tx.drv
	return met
}

// String implements the fmt.Stringer.
func (met *MasterEventType) String() string {
	var builder strings.Builder
	builder.WriteString("MasterEventType(")
	builder.WriteString(fmt.Sprintf("id=%v, ", met.ID))
	builder.WriteString("name=")
	builder.WriteString(met.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(met.Description)
	builder.WriteString(", ")
	builder.WriteString("rules=")
	builder.WriteString(fmt.Sprintf("%v", met.Rules))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(met.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MasterEventTypes is a parsable slice of MasterEventType.
type MasterEventTypes []*MasterEventType
